ow to fix both (The Strategy)
Fix A: The "Version Number" (In explosion.js and game.js)

Instead of comparing two massive map arrays with JSON.stringify (which is very slow), we will use a simple number called mapVersion.

    In explosion.js: Whenever a box is broken, whoever broke it (not just the host!) increments a mapVersion number in the room state.

        Command: setState("mapVersion", getState("mapVersion") + 1, true);

    In game.js: Inside the tick, only call buildFromMap if the mapVersion on the network is different than the one you are currently looking at.

Fix B: Remove the "Host Only" Restriction

In a game without a dedicated server, you should trust whoever broke the box to update the map.

Update explosion.js:
Remove the if (isHost()) check. If an explosion happens and mapChanged is true, call setState("map", ...) regardless of who you are.
Your To-Do List (Debugging and Tweaking):

1. Open explosion.js:

    Find the scheduleTask at the bottom.

    Fix the Typo: You have MapChanged (capital M) and mapChanged (lowercase m). Pick one and use it everywhere in that function!

    Remove isHost(): Let everyone update the map when they break a box.

2. Open game.js:

    Instead of syncing the whole map every second, can you think of a way to only call buildFromMap when a player specifically joins or when a box specifically breaks?

    Pro-Tip: If you move the buildFromMap call out of the 1-second timer and only trigger it when a bomb actually explodes, your bombs will stop disappearing!

Try fixing the variable names and the isHost check in explosion.js first. That will stop the boxes from respawning. Then, try disabling that 1-second mapSyncTimer in game.js to see if your bombs stay on the screen!